# Nuxt + PostgreSQL

AppThrust application template. The container listens on port 3000.

## Local Docker check

```bash
docker build -t template-nuxt:local .
docker run --rm -p 3000:3000 template-nuxt:local
```
